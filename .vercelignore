# Only the engine, the weights and the page are needed to serve the site.
games/
data/
runs/
web/
kaggle/
tests/
.venv/
*.pgn
*.log
reply*.txt
kaggle-fix.txt

# The connectome itself deploys (model/fly.npz, NumPy only). These are the
# PyTorch-dependent pieces, needed only to regenerate the weights locally.
model/fly.pt
fastchess/fly_engine.py
fastchess/fly_export.py
fastchess/fly_convert.py
